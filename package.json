{
  "name": "advent-of-code-fw",
  "private": true,
  "version": "1.0.0",
  "description": "Advent of Code Framework",
  "main": "index.ts",
  "scripts": {
    "build": "npm run clean && npm run copy-files && tsc",
    "watch": "tsc --watch",
    "clean": "rimraf dist/",
    "copy-files": "copyfiles -u 1 src/**/*.html src/**/*.txt src/**/*.css src/assets/**/*.* dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "browser": {
    "fs": false,
    "os": false,
    "path": false
  },
  "dependencies": {
    "fs": "^0.0.1-security",
    "typescript": "^5.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.19",
    "copyfiles": "^2.4.1",
    "rimraf": "^5.0.5"
  }
}
